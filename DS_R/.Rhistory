ls()
dir()
getwd()
rnorm
a = c(1,2,3,4,5,6,7,7,7,7,7,7,5,5,5,5,4,)
a = c(1,2,3,4,5,6,7,7,7,7,7,7,5,5,5,5,4,90)
mean(a)
median(a)
range(10,50)
series
rep(10,10)
seq(20,40)
seq(20,40,2)
seq(20,40,2)
seq(20,40,3)
22 in seq(20,40,3)
in
getwd()
setwd("E:/DS_R")
dir()
BlackFriday = read.csv("BlackFriday.csv")
summary(BlackFriday)
summary(BlackFriday)
head(BlackFriday)
mtcars
by_cyl = group_by(mtcars,cyl)
crs = select()
library(dplyr)
install.packages(dplyr,dependencies = TRUE)
install.packages(dplyr)
install.packages('dplyr')
library(dplyr)
str(group_by)
crs = group_by(mtcars,cyl)
crs
by_crs = summarise(crs,qsec=sum(qsec))
by_crs
crs
typeof(crs)
class(crs)
crs = aggregate(mtcars,by=list(cyl))
crs = aggregate(mtcars,by=list(cyl),FUN = sum, na.rm = TRUE)
crs = aggregate(mtcars,by=list(cyl),FUN = sum, na.rm = TRUE)
crs = aggregate(mtcars,by=cyl,FUN = sum, na.rm = TRUE)
crs = aggregate(mtcars,by=mtcars$cyl,FUN = sum, na.rm = TRUE)
crs = aggregate(mtcars,by=list(mtcars$cyl),FUN = sum, na.rm = TRUE)
crs
crs = aggregate(mtcars,by=list(mtcars$cyl,mtcars$vs),FUN = sum, na.rm = TRUE)
crs
crs = aggregate(mtcars,by=list(mtcars$cyl),FUN = sum, na.rm = TRUE)
crs
head(BlackFriday)
byuid = aggregate(BlackFriday, by=list(BlackFriday$User_ID),FUN=sum)
BlackFriday[,1]
BlackFriday$User_ID = as.factor()
BlackFriday$User_ID = as.factor(BlackFriday$User_ID)
summary(BlackFriday)
byuid = aggregate(BlackFriday, by=list(BlackFriday$User_ID),FUN=sum)
blacksub = as.data.frame(BlackFriday$User_ID,BlackFriday$Purchase)
blacksub = as.data.frame(BlackFriday$User_ID)
blacksub
blacksub$purchase = BlackFriday$Purchase
head(blacksub)
bsubagg = aggregate(blacksub,by=list(blacksub$`BlackFriday$User_ID`), FUN = sum)
